export enum Paths {
    CHARACTERS = 'characters',
    FAVORITES = 'favorites',
    DETAILS = 'details',
    LOGIN = 'login',
    REGISTER = 'register',
    LOGOUT = 'logout',
    INTERNAL_ERROR = 'internalError'
}